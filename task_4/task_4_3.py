import os
from dotenv import load_dotenv

load_dotenv()

db_url = os.getenv("DB_URL")
api_key = os.getenv("API_KEY")

print(f"Database URL: {db_url}")
print(f"API Key: {api_key}")

print("\n--- Пояснення ---")
print("Зберігання конфіденційних даних, таких як паролі, безпосередньо в коді є великим ризиком для безпеки:")
print("1. Контроль версій: Код часто зберігається в Git, що робить секрети видимими для всіх, хто має доступ.")
print("2. Випадкове розкриття: Секрети можуть легко потрапити в логи, повідомлення про помилки або спільні фрагменти коду.")
print("3. Складне управління: Зміна жорстко закодованого пароля вимагає зміни коду та повторного розгортання.")
print("Використання змінних оточення відокремлює конфігурацію від коду, роблячи додаток більш безпечним і портативним.")